[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "acmc-finance-utils"
version = "1.0.0"
description = "Utilitaires finance ACMC — sync notes CRM"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
